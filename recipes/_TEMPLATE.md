# <recipe title — imperative, e.g. "Add native widgets">

> **Source:** `templates/swift/Seed/path/to/gold-standard.swift` — cite the template path in this repo that ships the pattern, and confirm it against `portfolio/REUSE_INDEX.md` (link it with the correct relative path for this recipe's location: `../../portfolio/REUSE_INDEX.md` from `swift/` or `rn/`, `../portfolio/REUSE_INDEX.md` from the `recipes/` root). If the template doesn't ship the code yet, describe the pattern inline and mark the recipe ⚠️ pattern-only.
> **Platform:** Swift | React Native | Both
> **Reliability:** ✅ gold-standard / ⚠️ has caveats (explain)

## What it adds

One paragraph. What capability lands in the app, and what the user gets. No marketing — just the mechanism.

## When to use

- Legitimate trigger 1.
- Legitimate trigger 2.
- Legitimate trigger 3.

## When NOT to use

- Anti-pattern 1 — and what to do instead.
- Anti-pattern 2 — and what to do instead.
- The "looks easy so it gets over-applied" failure mode for this feature.

> This section is mandatory and is the **first thing the reader should read**. A recipe without a real `When NOT to use` is a footgun.

## How

### 1. Harvest

- Source: `templates/...` path — read it fully before adapting.
- Copy into: `<app>/path/in/this/app`.

### 2. Wire

- Edit step.
- Edit step.

### 3. Verify

```sh
# the exact command that proves it works
```

Expected: <what a passing run looks like>.

## Gotchas

- Known sharp edge 1.
- Known sharp edge 2.
